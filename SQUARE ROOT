#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    float t=0.0,s=0.0;
    s=n/2;
    while(s!=t)
    {
        t=s;
        s=((n/t+t)/2);
    }
    if((int)s==s)
    {
        printf("%d",(int)s);
    }
    else
    {
        printf("%.3f",s);
    }
    
}
