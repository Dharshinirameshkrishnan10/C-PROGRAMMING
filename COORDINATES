 #include <stdio.h>

int main() {
    int x, y;
    scanf("%d %d", &x, &y);

    if (x == 0 && y == 0) {
        printf("Origin\n");
    } else if (x == 0) {
        if (y > 0) {
            printf("Positive Y-Axis\n");
        } else {
            printf("Negative Y-Axis\n");
        }
    } else if (y == 0) {
        if (x > 0) {
            printf("Positive X-Axis\n");
        } else {
            printf("Negative X-Axis\n");
        }
    }

    return 0;
}
 XY coordinate
